# Python 3.11+
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- core data handling ---
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # parquet for the merged corpus

# --- config / secrets ---
python-dotenv>=1.0

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- stats / validation against DW-NOMINATE ---
scipy>=1.13
statsmodels>=0.14

# --- LLM APIs (ensemble; final composition still open, see CLAUDE.md) ---
openai>=1.40
anthropic>=0.34
google-genai>=0.3
tiktoken>=0.7          # token counting for cost estimates

# --- data acquisition ---
requests>=2.32
tqdm>=4.66

# --- dev tooling ---
black>=24.4
ruff>=0.5
pytest>=8.2
jupyter>=1.0
