# Python 3.11+
#
# Direct dependencies only, with the loosest range that works. This file says
# WHAT the project needs; requirements.lock.txt says which exact versions the
# team is running. Install from the lock, not from here:
#
#     make setup                    # or, by hand:
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.lock.txt
#
# Adding a dependency: add it here, then `make lock` and commit both files.

# --- core data handling ---
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # parquet for the merged corpus

# --- config / secrets ---
python-dotenv>=1.0

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- stats / validation against DW-NOMINATE ---
scipy>=1.13
statsmodels>=0.14

# --- LLM APIs (ensemble fixed: DeepSeek R1 / GPT-4o / Claude 3.5 Sonnet) ---
openai>=1.40           # GPT-4o, and DeepSeek via its OpenAI-compatible API
anthropic>=0.34        # Claude 3.5 Sonnet
tiktoken>=0.7          # token counting for cost estimates
tenacity>=8.2          # retry/backoff on rate limits and transient errors

# --- data acquisition ---
requests>=2.32
tqdm>=4.66

# --- dev tooling ---
black>=24.4
ruff>=0.5
pytest>=8.2
jupyter>=1.0
